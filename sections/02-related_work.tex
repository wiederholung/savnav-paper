\section{Related Work}
\label{sec:related}

\subsection{Vision-Based Social Navigation}
Traditional and learning-based SocialNav systems optimize paths to prevent physical collisions and respect implicit social norms~\cite{PrinciplesGuidelinesEvaluating2025}. Trajectory forecasting approaches proactively yield space based on future pedestrian states~\cite{CognitionPrecognitionFutureaware2025}, while recent foundation models distill human-inspired social compliance into unified navigation policies~\cite{SocialNavTrainingHumanInspired2026} or employ vision-language models (VLMs) to evaluate path etiquette~\cite{VLMsocialnavSociallyAware2025, ObstaclesEtiquetteRobot2026}. However, these vision-centric policies inherently depend on LOS sensors. Unable to perceive NLOS risks, they fail to anticipate a dynamic pedestrian approaching from a blind corner or yield to unobserved human activities, often causing abrupt boundary violations when entities suddenly fall into LOS.

\subsection{Audio-Visual Navigation}
AVNav equips agents with omnidirectional auditory perception to overcome restricted visual fields. Initial research proved sound effectively guides agents to static acoustic targets in unexplored spaces~\cite{LookListenAct2020, LearningSetWaypoints2021}. To handle realistic audio, subsequent frameworks relax the continuous-emission assumption to navigate using transient semantic sounds~\cite{SemanticAudiovisualNavigation2021}, track moving sources~\cite{CatchMeIf2023}, and tackle multi-source interference~\cite{AudiovisualNavigationNoisy2025}. Advanced architectures also introduce conversational agents to query oracles~\cite{AVLENAudiovisuallanguageEmbodied2022, CAVENEmbodiedConversational2024} or leverage large language models (LLMs) for zero-shot exploration and layout reasoning~\cite{RILAReflectiveImaginative2024}. However, these approaches treat non-target sounds exclusively as alternative goals or background noise to be attenuated. Consequently, they fail to model social sound events as spatial boundaries, leaving agents prone to disturbing stationary conversations or colliding with NLOS dynamic pedestrians while over-optimizing to reach the acoustic target.

\subsection{Multimodal Spatial Mapping}
Connecting acoustic signals with spatial reasoning relies on multi-modal perception. Foundation models like contrastive language-audio pretraining (CLAP)~\cite{CLAPLearningAudio2023} and ImageBind~\cite{ImageBindOneEmbedding2023} provide zero-shot cross-modal alignments, driving the evolution of open-vocabulary sound event localization and detection (SELD) systems~\cite{OpenvocabularySoundEvent2025}. Meanwhile, purely acoustic SELD has scaled through large-scale pre-training~\cite{PSELDNetsPretrainedNeural2025} and joint distance estimation~\cite{ExperimentalStudyJoint2025}, while audio-visual variants leverage visual cues to suppress reverberations~\cite{MVANetMultistageVideo2025}. Furthermore, recent works leverage the reasoning capabilities of LLMs to perform acoustic scene understanding~\cite{BATLearningReason2024} and the segmentation capabilities of foundation models to isolate target sounds from complex mixtures~\cite{SAMAudioSegment2025}. Despite their perceptual strength, these feature extractors focus on instantaneous event analysis rather than projecting sounds into persistent spatial constraints for navigation.

To support navigation, spatial mapping frameworks aggregate sensory features into dense fields or sparse topological structures~\cite{3DDynamicScene2020, ConceptFusionOpensetMultimodal2023, ConceptGraphsOpenvocabulary3D2024, ClioRealtimeTaskdriven2024, BareQueriesOpenvocabulary2025, M3DMapObjectawareMultimodal2025, DynamemOnlineDynamic2025}. Among these, few incorporate auditory modalities. While MM-OR~\cite{MMORLargeMultimodal2025} provides a multimodal dataset, AVLMaps~\cite{MultimodalSpatialLanguage2025} and HomeEmergency~\cite{HomeEmergencyUsingAudio2025} construct audio-inclusive maps. However, AVLMaps lacks the ability to infer distant audio sources; it registers sound at the agent's current location when a decibel threshold is exceeded. This degenerates omnidirectional hearing into simple proximity-based audio logging and assumes a static environment. Meanwhile, HomeEmergency updates probability-based scene graphs using audio, but it relies on idealized audio directionality assumptions and predefined human-activity heatmaps to localize audio events, which are difficult to realize.

% SAVMap addresses these limitations without relying on strict prior constraints. It uses acoustic spatial perception to anchor transient sound events into the topological layout as inferred entities. By projecting these non-visual inferences into a continuous social cost field, SAVMap transforms multidirectional acoustic cues into persistent, geometry-aware boundaries for proactive planning.
